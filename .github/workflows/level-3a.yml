#############################################
# Level 3a — Boo's Haunted Mansion
#
# Doel:
#  Ontsnap aan Boo's spookhuis met de kracht van Quality Gates.
#
# Daadwerkelijk doel:
# - Coverage meten met JaCoCo
# - Een minimale coverage‑drempel afdwingen (quality gate)
#
# Let op:
# - Voeg eerst de JaCoCo plugin toe in build.gradle (zie level-3-README.md)
#############################################
name: Level 3a - Boo's Haunted Mansion

on:
  workflow_dispatch: {}

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

        # TODO - zelf laten uitzoeken (hier jacoco op 50%)

      - name: Run tests + generate JaCoCo report
        run: # TODO: Zorg dat je een jacocoTestReport kan genereren met gradle en laat de job falen bij een dekking < 60%

      - name: Upload coverage reports
        # TODO: Zorg dat de coverage raporten worden geupload - hint: zie de laatste stap in level 2